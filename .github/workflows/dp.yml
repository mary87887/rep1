name: DP
on:
  push:
  schedule:
    - cron: '0 0 1 1-12 *'

jobs:
  ok-dp:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: dp
      run: |
        docker run -d mary887887/centos:202011
        sleep 700h
